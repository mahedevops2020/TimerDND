⏱️ Timer DND

Timer DND is a simple and efficient Android timer application that automatically manages your phone’s Do Not Disturb (DND) mode.
When the timer starts, the app enables DND to silence interruptions. When the timer is stopped or reset, the original sound profile is restored.

✨ Features

Automatic DND Management
Automatically enables Do Not Disturb mode when the timer starts and disables it when the timer stops or resets.

Simple Timer Interface
Displays elapsed time with hours, minutes, seconds, and milliseconds.

Standard Controls
Includes Start, Stop, and Reset buttons for easy operation.

Keep Screen On
Prevents the device from sleeping while the timer is running.

Dark Theme UI
Uses a dark, battery-efficient design, ideal for OLED screens.

Permission Handling
Gracefully requests the required DND permission on first use and redirects users to system settings if needed.

🛠 Technology Stack

Language: Kotlin

Framework: Native Android SDK

UI: Android XML Layouts

Build Tool: Gradle

🔐 Permissions

This app requires the following permission:

ACCESS_NOTIFICATION_POLICY
Used to control the system’s Do Not Disturb mode.

The app will guide users to the appropriate system settings screen to grant this permission when required.

🚀 How to Build & Run

Clone the repository

git clone https://github.com/mahedevops2020/TimerDND.git


Open the project in Android Studio.

Sync Gradle dependencies.

Build and run the app on an emulator or a physical Android device.
